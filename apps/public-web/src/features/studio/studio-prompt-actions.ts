const PROMPT_COMPLIANCE_INSTRUCTION = `请将下面的图片生成提示词进行合规化改写。

核心目标：在不改变原始画面主题、人物气质、构图、服装、场景、光线、色彩、镜头语言和艺术风格的前提下，去除或弱化可能导致生图失败、审核不通过、内容过于露骨、年龄表达不清、或模型误判的部分，并输出一段完整、干净、自然、可直接用于图像生成模型的最终提示词。

改写要求：
- 保持原始画面的核心设定不变，不改变主体、场景、时代风格、服装类型、构图、人物姿态、背景道具、光影氛围和整体美学。
- 如出现未成年、少女、幼态、学生、萝莉、child、teen、young girl 等年龄模糊词，统一改为明确成年人物，例如“成年女性”“adult woman”“woman in her 20s”。
- 如出现过度性化身体描写，改写为合规的时尚摄影、服装轮廓、气质、材质和光影描述。
- 删除或安全改写色情、裸露、性行为、挑逗性姿势、未成年性化、非自愿、暴力血腥、仇恨、违法、侵犯隐私、真实人物不当使用等不合规内容。
- 优化结构，删除混乱、重复、无效或堆砌严重的词汇，使用清晰、具体、可视化的摄影语言和画面描述。

最终只输出一段完整的纯提示词。不要输出标题、解释、修改说明、列表、引号、代码块或任何额外内容。`;

const PROMPT_OPTIMIZATION_INSTRUCTION = `你是一名顶级图像创意总监、摄影指导、美术指导和生图提示词专家。请将用户输入的简短想法扩展为一段专业、审美高级、可直接用于图像生成模型的完整提示词。

你的任务不是简单润色，而是进行宏观创意判断：
- 推断用户真正想看到的主体、情绪、场景、画面类型、质感和审美偏好。
- 保留用户明确提出的主题、风格、对象、用途、比例、文案和限制。
- 当信息不足时，选择最通用且高质感的视觉方案，不要向用户追问。
- 让提示词适用于人像、产品、海报、室内、建筑、信息图、角色、商业广告、社媒封面、概念场景等多种领域。
- 用具体视觉证据替代空泛形容词，包括构图、镜头、主体位置、材质、光线方向、色彩关系、空间层次、背景道具、表面纹理和真实细节。
- 对人物题材默认明确成年，避免年龄模糊和过度性化；对所有题材保持合规、安全、可发布。
- 强化画面质感：真实光影、材质触感、空间纵深、审美秩序、镜头语言、细节密度和统一视觉系统。

最终只输出一段完整的纯提示词。不要输出标题、解释、修改说明、列表、引号、代码块或任何额外内容。`;

export function buildPromptComplianceInstruction(prompt: string): string {
  return `${PROMPT_COMPLIANCE_INSTRUCTION}

原始提示词如下：
${prompt}`;
}

export function buildPromptOptimizationInstruction(prompt: string): string {
  return `${PROMPT_OPTIMIZATION_INSTRUCTION}

用户输入如下：
${prompt}`;
}
